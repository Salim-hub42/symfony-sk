{% extends 'base.html.twig' %}

{% block title %}Profil de {{ user.prenom|default('Utilisateur') }}{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card shadow-lg border-0 rounded-4">
                <div class="card-header bg-success text-white rounded-top-4">
                    <h2 class="mb-0 text-center">👤 Fiche utilisateur</h2>
                </div>
                <div class="card-body p-4">
                    <div class="text-center mb-4">
                        <span style="font-size:64px;">👤</span>
                        <h4 class="mt-3">{{ user.prenom|default('Utilisateur') }} {{ user.nom|default('') }}</h4>
                    </div>
                    <ul class="list-group list-group-flush mb-4">
                        <li class="list-group-item"><strong>Nom :</strong> {{ user.nom }}</li>
                        <li class="list-group-item"><strong>Prénom :</strong> {{ user.prenom }}</li>
                        <li class="list-group-item"><strong>Email :</strong> {{ user.email }}</li>
                        {# Ajoutez d'autres champs si besoin, ex: email, rôles... #}
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}